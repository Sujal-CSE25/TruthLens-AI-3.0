# ─────────────────────────────────────────────
# TruthLens Border Intelligence — Python Backend Dependencies
# SIH 2026 · PS 26188 · PROTOTYPE — NOT FOR OPERATIONAL USE
# ─────────────────────────────────────────────

# Environment & Config
python-dotenv==1.2.3

# API Server Layer
fastapi==0.141.1
uvicorn[standard]==0.52.4
python-multipart==0.0.32
httpx==0.28.1

# LLM & Reasoning Engine (Google Gemini SDK)
google-genai==2.8.0

# Scientific, Statistical & Data Processing
numpy==1.26.4
scipy==1.14.1

# Image Forensics (ELA, EXIF, Noise Analysis)
Pillow==10.4.0
piexif==1.1.3

# Document & Credential Forensics (OCR & PDF Extraction)
pytesseract==0.3.13
pypdf==5.0.1
rapidocr-onnxruntime==1.4.4

# ─── PS 26188 — Border Intelligence Dependencies ─────────────
# NLP embedding (existing — used for claim clustering)
sentence-transformers==3.0.1

# Face detection (OpenCV) — for Haar cascade face detection
opencv-python-headless==4.10.0.84

# Face biometric verification — real embedding, DeepFace
# NOTE: deepface is OPTIONAL. If not installed, face verification returns UNAVAILABLE
# (not a fake score). Install manually: pip install deepface tf-keras
# deepface==0.0.93   # commented out — install manually to avoid large TF dependency

# Existing NLP/embedding stack (already had sentence-transformers)
torch>=2.0.0; platform_machine!="aarch64"  # optional — for sentence-transformers GPU

